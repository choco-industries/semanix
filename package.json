{
  "name": "semanix",
  "version": "0.0.1",
  "workspaces": [
    "client",
    "backend",
    "common"
  ],
  "scripts": {
    "build:common": "bun run --cwd common build",
    "dev:client": "bun run --cwd client dev",
    "dev:backend": "bun run --cwd backend dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "bun": "^1.2.3"
  }
}
